class User{
    constructor(first, last, mail) {
        this.name = first;
        this.lname = last;
        this.mail = mail;
    }

    // showInfo(){
    //     console.log(`${this.name}:${this.lname}`)
    // }
}

export default User;